spring:
  #스프링 부트는 별도의 설정이 없으면 메모리 모드로 DB를 돌림
#  datasource:
#    url: jdbc:h2:mem:test # 메모리 모드로 동작
#    username: sa
#    password:
#    driver-class-name: org.h2.Driver
#
#  jpa:
#    hibernate:
#      ddl-auto: create # 엔티티를 먼저 드랍하고 크리에이트 하고 애플리케이션 등록
#                create-drop # 애플리케이션 종료시점에 모두 드랍
#    properties:
#      hibernate:
##        show_sql: true
#        format_sql: true

logging.level:
  org.hibernate.SQL: debug
  org.hibernate.type: trace